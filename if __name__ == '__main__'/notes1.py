print(__name__)  # -----> we get __main__

# python before it starts running, it sets few special variable, __name__ is one of them
# whenever we import modules, python sets __name__ to the file, which is in our case notes1.py
